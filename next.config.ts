import type { NextConfig } from "next";

const nextConfig: NextConfig = {
 images: {
    domains: ["raw.githubusercontent.com","res.cloudinary.com", "images.pexels.com", "images.unsplash.com"], // add this

  },
};

export default nextConfig;
